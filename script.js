const toggle=document.querySelector('.toggle'),nav=document.querySelector('.header nav');toggle?.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const compare=document.getElementById('compare');let drag=false;function move(x){const r=compare.getBoundingClientRect();const p=Math.max(0,Math.min(100,(x-r.left)/r.width*100));compare.style.setProperty('--split',p+'%')}compare?.addEventListener('pointerdown',e=>{drag=true;compare.setPointerCapture?.(e.pointerId);move(e.clientX)});compare?.addEventListener('pointermove',e=>drag&&move(e.clientX));['pointerup','pointercancel'].forEach(e=>compare?.addEventListener(e,()=>drag=false));
document.getElementById('form')?.addEventListener('submit',e=>{e.preventDefault();document.getElementById('toast').classList.add('show');e.target.reset();setTimeout(()=>document.getElementById('toast').classList.remove('show'),3500)});